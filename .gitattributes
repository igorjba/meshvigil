# Normalise line endings to LF in the repo so CI (Linux) and local (Windows) agree.
* text=auto eol=lf

# Treat these as binary — never touch their bytes.
*.png binary
*.webm binary
*.woff binary
*.woff2 binary
